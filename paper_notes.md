- use the visibility calcualtion from the PUN paper as the target to compare per-view and cross-view performance, since it can be pre-computed and doesn't require expensive rendering (compare how the the PUN NUM dataset was calculated).

