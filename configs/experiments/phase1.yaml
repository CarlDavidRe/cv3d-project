schema_version: 1

experiment:
  phase: phase1
  name: infrastructure
  seed: 0
  deterministic: true

paths:
  data_root: data/NUM
  output_root: outputs
  model_cache_root: data/cache/models

phase1:
  num_anchors: 48
  num_dataset:
    split_manifest: data/splits/num_v1.json
    target_name: PSNR
